#include "libft.h"

void ft_lstclear(t_list **lst, void (*del)(void*))
{
    t_list *curr;

    curr = ft_calloc(1, sizeof(t_list));
    curr = *lst;
    while (curr->next != NULL)
    {
        
    }
    lst = NULL
}
